# ============================================================
# Basalt Environment Variables
#
# The blog reads from D1 (Cloudflare SQLite). Data is synced to D1
# by the sync worker (sync-worker/). The following API keys are only
# needed by the sync worker, NOT by the Pages app itself.
# ============================================================

# --- Sync Worker Secrets (set via `wrangler secret put` in sync-worker/) ---
# These are NOT needed by the Pages app — only the sync worker.

# Notion API Configuration
# Get your API key from https://www.notion.so/my-integrations
NOTION_API_KEY=secret_xxx

# Database IDs (find in Notion database URLs)
NOTION_ARTICLES_DATABASE_ID=xxx
NOTION_PHOTOS_DATABASE_ID=xxx

# Optional: Telegram Channel for microblog (public channels only)
# Set the channel username (without @)
TELEGRAM_CHANNEL_USERNAME=your_channel_name

# Optional: Custom RSSHub instance (default: https://rsshub.app)
RSSHUB_INSTANCE=https://rsshub.app

# Optional: Douban/NeoDB RSS feed URL
DOUBAN_USER_RSS=https://www.douban.com/feed/people/YOUR_USER_ID/

# --- Pages App Configuration (public, safe for client) ---

# Optional: Sync worker URL for manual sync trigger via /api/cache-clear
# Set this to your deployed sync worker URL
SYNC_WORKER_URL=https://basalt-sync.your-subdomain.workers.dev

# Optional: Shared secret for sync trigger endpoint
SYNC_WORKER_TOKEN=

# Optional: Giscus Comments Configuration
# Get these values from https://giscus.app/zh-CN after configuring your repo
# Your repo must be public and have Discussions enabled
PUBLIC_GISCUS_REPO=your-username/your-repo
PUBLIC_GISCUS_REPO_ID=R_xxxxx
PUBLIC_GISCUS_CATEGORY=Announcements
PUBLIC_GISCUS_CATEGORY_ID=DIC_xxxxx
# Custom theme URL (point to your hosted CSS file)
PUBLIC_GISCUS_THEME_URL=https://raw.githubusercontent.com/your-username/your-repo/main/public/giscus-theme.css

# Optional: Webmention Configuration (webmention.io hosted)
# Register at https://webmention.io and set your username (usually your domain)
PUBLIC_WEBMENTION_USERNAME=your-domain.com
# IndieAuth verification URL (your GitHub/Mastodon/email profile)
PUBLIC_INDIEAUTH_GITHUB=https://github.com/your-username
