name = "basalt"
compatibility_date = "2024-01-01"

# D1 database binding — the blog's single source of truth
# Run `wrangler d1 create basalt` to get the ID, then replace below.
[[d1_databases]]
binding = "DB"
database_name = "basalt"
database_id = "c4180715-68e4-4d01-8cf7-a3e0ce5bb960"
migrations_dir = "migrations"
